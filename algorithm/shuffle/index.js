/**
 * @function shuffle
 * @description 洗牌算法，将一个数组的元素随机打乱后返回
 * @param {Array} inputArray
 * @returns {Array} 打乱后的新数组
 */

function shuffle(inputArray) {
	// 请实现 shuffle 方法
	// 可以 require 任何依赖模块
}

module.exports = shuffle;