var should = require('should');

var shuffle = require('./index');

describe('shuffle', function() {
	it('shuffle should be a function.', function() {
		(typeof shuffle).should.equal('function');
	});
	// 下面请测试你的 shuffle 模块
});